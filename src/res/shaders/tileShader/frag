#version 330 core

in vec2 worldPosition;

out vec4 color;

uniform sampler2D texSampler;

void main() {
    color = texture(texSampler, vec2(mod(worldPosition.x, 5) / 5, mod(worldPosition.y, 5) / 5));
}